
cmake_minimum_required(VERSION 3.0.0)
project(matam VERSION 0.1.0 LANGUAGES C)

set(MTM_FLAGS_DEBUG "g++ -std=c++11 -Wall -Werror -pedantic-errors -DNDEBUG -o progA -IpartA
-Iprovided provided/test_partA.cpp partA/*.cpp -Lprovided -ldate
")
set(MTM_FLAGS_RELEASE "${MTM_FLAGS_DEBUG} -DNDEBUG")

set(CMAKE_C_FLAGS ${MTM_FLAGS_DEBUG})

//(.)link_directories
include_directories(provided)
add_executable(final_executable ./date.h ./date_wrap.h ./date_wrap.cpp)
target_link_libraries(final_executable ./libdate.a)

